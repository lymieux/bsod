```
Blue Screen of Death (bsod):

Building:
~~~~~~~~~
    Requirements:
    + Cmake 
    + Visual Studio

    Instructions: (in git repo root directory)
        Building:
            mkdir .\build\
            cd .\build\
            cmake ..
            cmake --build . --config Release
        Running: (assuming in build directory above)
            .\Release\bsod.exe
```
